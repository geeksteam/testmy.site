import React, { Component } from 'react';

import SiteForm from "./../SiteForm/SiteForm";
import './App.css';

class App extends Component {
  render = () => {
    return (
      <SiteForm />
    );
  }
}

export default App;
